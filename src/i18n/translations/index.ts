import { Translations } from '../../types/i18n';
import { ru } from './ru';
import { en } from './en';
import { uz } from './uz';

/**
 * Translations map for all supported languages.
 */
export const translations: Record<string, Translations> = {
  ru,
  en,
  uz,
};

