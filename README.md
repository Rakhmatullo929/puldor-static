# Puldor — AI Telegram bot for voice-based expense tracking

Демо-лендинг для конкурса **AI500, Этап 1**.

Puldor — умный Telegram-бот для учёта расходов через голос. Просто скажи, сколько потратил — AI сам поймёт, разложит по категориям, учтёт доходы, цели, долги и подскажет, где ты можешь экономить.

## 🚀 Технологии

- **React 18** + **TypeScript** — UI библиотека и типизация
- **Vite** — быстрый сборщик и dev-сервер
- **Tailwind CSS** — utility-first CSS фреймворк
- **Inter font** — современный шрифт для лучшей читаемости

## 👥 Команда

Команда из 4 человек, закрывающая продукт от ИИ-ядра до питчинга на английском:

1. **Backend / Tech Lead** — проектирование API и архитектуры системы, опыт работы с high-load системами, интеграция с Telegram Bot API и платежными системами
2. **AI / ML Engineer** — разработка AI-пайплайнов для обработки голосовых сообщений и текста, классификация транзакций, интеграция с Whisper и GPT-4.x
3. **Backend Engineer (Product features)** — разработка продуктовых фич: цели, долги, аналитика расходов, оптимизация запросов к БД
4. **Native-speaker / Pitch & Product** — IELTS 8.5, отвечает за питчинг проекта, продуктовую логику и UX-тексты на английском языке

## 📦 Установка

```bash
npm install
```

## 🛠️ Разработка

Запуск dev-сервера с hot-reload:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## 🏗️ Сборка

Сборка production-версии:

```bash
npm run build
```

Результат сборки будет в папке `dist/`.

Предпросмотр production-сборки:

```bash
npm run preview
```

## 🚢 Деплой

### Vercel

1. Подключите репозиторий к Vercel
2. Настройки:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
   - **Node.js Version**: 18.x или выше
3. Деплой произойдёт автоматически при push в основную ветку

### Netlify

1. Подключите репозиторий к Netlify
2. Настройки:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - **Node version**: 18 или выше
3. Деплой произойдёт автоматически при push в основную ветку

### Другие платформы

Для деплоя на другие платформы (GitHub Pages, Cloudflare Pages и т.д.):

1. Убедитесь, что Node.js версии 18+ установлен
2. Выполните `npm run build`
3. Загрузите содержимое папки `dist/` на хостинг

## 📁 Структура проекта

```
src/
├── components/
│   ├── layout/
│   │   ├── Header.tsx      # Навигация и шапка сайта
│   │   ├── Footer.tsx      # Подвал сайта
│   │   └── Layout.tsx      # Обёртка для всего приложения
│   └── sections/
│       ├── Hero.tsx                    # Главная секция с CTA
│       ├── ProblemSolution.tsx         # Проблема и решение
│       ├── Team.tsx                    # Команда
│       ├── WhyUs.tsx                   # Почему именно мы
│       ├── Roadmap.tsx                 # Дорожная карта
│       └── Implementation.tsx           # Как мы планируем реализовать
├── App.tsx          # Главный компонент приложения
├── main.tsx         # Точка входа
└── index.css        # Глобальные стили и Tailwind директивы
```

## 🎨 Особенности

- ✅ Полностью адаптивный дизайн (мобильные, планшеты, desktop)
- ✅ Плавные анимации при hover и скролле
- ✅ SEO-оптимизация с мета-тегами и Open Graph
- ✅ Плавная прокрутка между секциями
- ✅ Современный UI с градиентами и тенями

## 📝 Скрипты

- `npm run dev` — запуск dev-сервера
- `npm run build` — сборка production-версии
- `npm run preview` — предпросмотр production-сборки
- `npm run lint` — проверка кода линтером

## 🔗 Ссылки

- [AI500 Competition](https://ai500.uz/) — официальный сайт конкурса
- [Telegram Bot API](https://core.telegram.org/bots/api) — документация Telegram Bot API

## 📄 Лицензия

Проект создан для участия в конкурсе AI500.
# puldor-static
